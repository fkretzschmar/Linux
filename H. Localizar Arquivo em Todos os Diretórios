#Localizar arquivo em todos os diretórios
find / -name *.com
sudo find / -name *.com

#Localizar arquivo apartir do diretório etc
sudo find /etc -maxdepth 2 -name *.conf
Obs: -maxdepth vai mostrar o caminho/quantidade de diretórios que você deseja ver até o arquivo.

#Localizar Arquivo por Tamanho
sudo find / -size +100M
